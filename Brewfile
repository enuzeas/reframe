# ponytail: ffmpeg/mediamtx aren't used by any code yet (M3 송출 PoC) — listed here
# now since M2 is exactly one step before M3 needs them. See docs/INFRA-PLAN.md §10.
brew "ffmpeg"
brew "mediamtx"

# NDI SDK runtime (libndi.dylib) for --ndi-out (cyndilib in pyproject.toml links against it).
# Not brew-installable (proprietary redistributable) — install via NDI Tools:
# https://ndi.video/tools/ (this machine already has it from a prior NDI Tools install).
